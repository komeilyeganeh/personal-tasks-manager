import { Grid } from "@mui/material";
import { FC, ReactElement } from "react";

export const TextArea: FC = (): ReactElement => {
  return (
    <Grid item md={8} px={4}>
      <h2>Content</h2>
    </Grid>
  );
};
